/**
 * 
 * Check if a string is Colindrome Given a string, check if it is Colindrome or
 * not. A string is said to be colindrome if it has consecutive 3 alphabets
 * followed by the reverse of these 3 alphabets and so on.
 * 
 * Examples :
 * 
 * Input : cappaccappac Output : String is colindrome
 * 
 * Input : mollomaappaa Output : String is Colindrome
 * 
 * Approach : Take two empty strings s1 and s2, and start iterating over the
 * given string. Take first three letters of the string and store it in s1, next
 * three chars in s2 and then match s1 and s2. If they are same then do the same
 * again till length of the given string else return false.
 * 
 */
public class CheckIfAStringIsColindrome {

	public static void main(String[] args) {

	}

}
